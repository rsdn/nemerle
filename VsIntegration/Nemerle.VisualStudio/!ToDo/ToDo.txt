1.  (Готово) Реализовать вывод информации о АСТ файла в докинг-окно "Ast Tool Window".
		Обновление окна делает на Check().
2.	Поженить новый инсталлятор с имеющимся. Добавить возможность задать каталог и выбрать компоненты. -> pbludov
3.	Генерация кода с метаданных. -> pbludov
4.  Надо сделать так чтобы хинты с сообщениями об ошибках включали все сообщения 
    приходящиеся на некоторый локейшон, а не только последний (как сейчас). -> IT
5.  Имеются некоторые глюки при комлите уточнений типов (type enforcement).
    Надо пофиксить это дело в компиляторе.
6.  Надо реализовать оптимизацию предотвращающую перестройку дерево типов в случае
    если редактирование только изменяет пробельные символы (не разбивает 
    значимые токены).
7.  Надо реализовать поках хинтов (по Ctrl+Space) для индексаторов и 
    параметров типов.
8.  (Готово) Надо реализовать подсветку парных скобок при приближении к одной из них курсора.
9.  (Готово) Надо реалзовать переход на парную скобку по Ctrl+}.
10. (Готово) Надо реализовать комлит в уточнениях типов (type enforcement) в членах классов
    отличных от методов (сейчас он работает только в них).
11. Опримизация! При редактировании вне тела метода надо постараться не пересоздавать
    дерево типов пока в этом не возникнет насущьной потребности. Вместо этого нужно
    пытаться произвести парсинг только текущего файла (получая только Decl 
    и TopDeclaration). В принципе практически все операции на вне тела методов 
    можно осуществлять на базе Decl и TopDeclaration. Это уберет зависимость
    скорости редактирования файла от размера проекта. Дерево типов по идее
    должно перестраиваться только на Check(), при попытке посмотреть хинты
    или произвести комплит в теле метода.
12. После престроения дерева типов остаются сообщения об ошибках в методах.
    Эти ошибки уже могут быть исправлены в следствии того, что изменилось дерево
    типов. Происходит это потому, что Check() вызывается только для измененного
    файла, а метод с ошибкой находится в другом файле. Надо как-то сделать
    теневой вызов Check() для всех файлов содержащих ошибки в методах
    после изменения дерева типов.
13. Надо реализовать отображение комбов навигации в файлах не подключенных к 
    проекту.
14. При комплите списков надо распозновать это и заменять Nil на [], а
    Cons на имя параметра с отрезанным суфиксом "s" (если таковой есть).
15. Реализовать комплит в парметрах ламд и локальных функций. Для этого надо 
    дорабатывать поддержку комплита в компиляторе. Сейчас вывод типов обрывается
    сразу при обнаружении того места где найден комлитящийся токен. Надо продолжать
    типизацию до победного конца.
16. (Готово, вроде бы) Хинт для полей из типов объявленных в других сборках выводится без указания 
    типа поля. Выводится он через ToString(). Надо бы это дело подправить.
17. (Готово) Глючит комплит типа "IO.Fi|". Не выдает File.
18. Реализовать считывание всех настроек из файла проекта.