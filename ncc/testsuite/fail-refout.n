using Nemerle.IO;

module A {

  f (x : ref int) : void // E: cannot store.*in closure
  {
    def g () {
      x = x + 3;
    };
    g ();
    g ();
  }

  Main () : void
  {
    def _g (_ : ref int) { };    // E: ref/out .*not supported
    ()
  }
}
