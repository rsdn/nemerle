
module M {

  public Main () : void {
    ala ();

    def _ = haha ();
    A.gg (Nemerle.IO.printf ("44"));
    
    mutable i <- 0;
    forpermutation (i in 3 to 10)
      Nemerle.IO.printf ("%d\n", i)
  }
}
