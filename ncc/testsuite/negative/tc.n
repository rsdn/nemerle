interface I
  {
    get () : int;
  }

class C ['a] where 'a : I {}

class M {
  x : C[int];    // E: typing constraint .* is not satisfied
}


class X ['a] {
  x : 'a;               // OK
  static y : 'a;        // E: unbound type
  m1 (_ : 'a) : void {}         // OK
  static m2 (_ : 'a) : void {}   // E: unbound type
  static m3['a] (_ : 'a) : void {}   // OK
}
