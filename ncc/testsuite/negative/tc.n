interface I
  {
    get () : int;
  }

class C ['a] where 'a : I {}

class M {
  x : C[int];    // E: typing constraint on.* not satisfied
}


class X ['a] {
  x : 'a;               // OK
  static y : 'a;        // OK
  m1 (_ : 'a) : void {}         // OK
  static m2 (_ : 'a) : void {}   // OK
  static m3['a] (_ : 'a) : void {}   // OK

  class B {
    foo (x : 'a) : void {} // OK
  }
}
