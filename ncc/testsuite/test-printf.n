
module M {
  public Main () : void {
    printf ("Ala %d ma %s i %s\n", 101010, "koat", "paes");
    printf ("Dziala%s czy %d nie %d %s\n", " mi to", 3, 4, "dziala?");
 
    mutable i <- 1;
    mutable n <- 1;
    while (i < 6, {
      printf("licze %d\n", n);
      i <- i + 1;
      n <- i * n;
    });

    mutable a <- 4;
    mutable b <- "";
    scanf ("%d %s", a, b);
    printf("liczba %d, napis %s\n", a, b);

  }
}

(*
BEGIN-OUTPUT
Ala 101010 ma koat i paes
Dziala mi to czy 3 nie 4 dziala?
licze 0
licze 1
licze 2
licze 6
licze 24
licze 120
END-OUTPUT
*)
