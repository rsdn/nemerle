module M {

  'a meth (v : int, x : 'a) : 'a {
    if (v < 0) x
    else {
      def (x, _) = meth (v - 1, (x, x));
      x
    }
  }

  Main () : void {
    printf ("%d\n", meth (5, 1));
  }
}

/*
BEGIN-OUTPUT
1
END-OUTPUT
*/
