module M {
  public Main () : void {
    def l = [(1,2);(2,3)];
    def f (x) {
      match (x) {
        | (_, 2) => true
        | _ => false
      }
    };
    when (Nemerle.Collections.List.exists (f, l)) print_endline ("ok");
  }
}
