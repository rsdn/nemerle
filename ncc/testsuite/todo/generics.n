
// this works :-)
class A ['a] {
  public mutable f : 'a;
  
  public take () : 'a {
    f
  }
  
  public put (x : 'a) : void {
    _ = A(1); // this works, because inside of current class?
    f = x;
  }
  
  public this (a : 'a) {
    f = a;
  }  
}

module M {
   Main () : void {
  // does not work, TypeBuilder.GetConstructor fails with strange exception
  // need to investigate 
  //       _ = A(1);
   }
}

