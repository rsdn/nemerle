using Nemerle.IO;

public class ArrayTest
{

  public static Main () : void 
  {
        def result = (System.Array.CreateInstance (1.GetType().GetType (), 2) :> array<object>);
//loop(result);

	def loop () {
      		result[0] = (typeof (int)); 
	};
loop ();
loop ();

    ()
  }
}

/*
BEGIN-OUTPUT
Array test.
a b 
5
1979
0
END-OUTPUT
*/
