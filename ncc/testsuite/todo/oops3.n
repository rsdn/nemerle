open Nemerle.Collections;

module M {
  public Main () : void {
    def f (a : int * int) : void {
      ()
    };

    f ((1,1));  // first problem

    def g (a) {
      def (b,c) = a;
      def g = b + 1 + c;
      ()
    };

    def y = [(1,2); (3,4)];
    List.iter (g, y)  // second problem
  }
}
