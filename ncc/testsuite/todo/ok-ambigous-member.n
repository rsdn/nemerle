open System.Text.RegularExpressions;

class foo{

public static Main():void
	{
	def regexp= Regex("//\\s*E:\\s*(?<1>.*)$");
	def str= "// E: blabla\n";
	def err_match=regexp.Match(str);
	if (err_match.Success){
	     def sss=err_match.Groups[1]; \\ OK
		()
		}
	else
		()
	}
}

(*
BEGIN-OUTPUT
END-OUTPUT
*)
