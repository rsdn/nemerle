using Nemerle.Collections;

module M {
  f (x : int) : int { x }
  f (_x : string) : int { 1 }
  g<'a> (l : list<'a>, f : 'a -> int) : void {
    ignore (List.Map (l, f))
  }

  public Main () : void {
    def l = [1,2,3];
    g (l, f)
  }
}
