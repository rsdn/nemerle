class C {
  public mutable x : int;
  public m () : void {}
}

class M {
  static main () : void {
    def c = (null : C);
    c.x <- 3; // OK
    3;    // W: ignored .*value
    Foo.Bar.baz;        // E: unbound symbol `Foo'
    M.quxx;           // E: unbound member `quxx' in type `M'
    c.y <- 3    // E: unbound member `y' in type `C'
  }

  static f (c : C) : void { c.m <- 3 }    // E: .*read-only
}
