using Nemerle.IO;

module A {

  f (x : ref int, y : out object) : void
  {
    x = x + 3;
    y = 42;
  }

  Main () : void
  {
    mutable x = 3;
    mutable y = null;
    f (ref x, out y);
    printf ("%d %s\n", x, y.ToString ());
  }
}

/*
BEGIN-OUTPUT
3 42
END-OUTPUT
*/
