module M {
  Main () : void {
    System.Console.Write ("foo" + "bar" + "\n");
    def z = System.Decimal (10 + 12) + System.Decimal (6 + 4 + 10);
    when (42 == System.Decimal.ToInt32 (z)) printf ("ok\n");
  }
}

/*
BEGIN-OUTPUT
foobar
ok
END-OUTPUT
*/
