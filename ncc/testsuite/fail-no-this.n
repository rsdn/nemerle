class Bar { public xx : int; }

class Foo : Bar {
  static xs : int;      // OK
  ms () : int { xs }    // OK
  
  xi : int;                     // OK
  mi () : int { xi + ms () }    // OK
  mi2 () : int { mi () + ms () }// OK

  static ms2 () : int { xi }            // E: unbound symbol `xi'
  static ms3 () : int { mi () }         // E: unbound symbol `mi'

  static new xx : int;                      // OK
  yy () : int { xx }                        // OK
  // was `ambiguity between `this.xx' and a static field `Foo.xx'`
}
