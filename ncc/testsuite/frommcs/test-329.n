using System;

[X (null)]
class X : Attribute {
	 mutable  ID : int;
	public this () {}
	public this ( o : object)
	{
	{ 
		when (o == null)
			ID = 55;
	}
	}
	
	static Main () :  int {
		mutable  attrs = typeof(X).GetCustomAttributes(typeof (X),false);
		if (attrs.Length != 1)
		{
			 2;
		}
		else {
	    
		mutable  x =(match ( attrs [0]) { _tmp :  X => _tmp | _ => null });
		if (x.ID != 55)
		{
			 2;
		}
		else {
	    
		Console.WriteLine("OK");
		 0;
	}
	}
	}
}
/*
BEGIN-OUTPUT
OK
END-OUTPUT
*/
