#!/bin/sh
ENGINE=`grep NET_ENGINE ../../config.mak | awk '{print $3}'`

rm -f out.exe
export MONO_PATH=../out.stage1/:../out.stage2/
time $ENGINE ../out.stage1/ncc.exe -ignore-confusion -no-stdlib -r Nemerle.stage1 -r:System "$@" || exit 1
time $ENGINE ./out.exe || echo "Non zero exit code."

