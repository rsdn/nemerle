#!/bin/sh

rm -f out.exe
time mono ../ncc.exe -ignore-confusion ../../lib/{aliases,core,list,ienumerable,ienumerator}.n "$@" || exit 1
#time mono ../../boot/ncc.exe ../../lib/{aliases,core,list}.n "$@" || exit 1
#mcs -nowarn:162 -o a.out out.cs 2>&1 | grep -v 'Compilation suc'
mono ./out.exe
#rm -f a.out out.cs
