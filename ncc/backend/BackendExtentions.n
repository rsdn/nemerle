using Nemerle.IO;
using Nemerle.Collections;
using Nemerle.Compiler.Typedtree;

using SR  = System.Reflection;
using SRC = System.Runtime.CompilerServices;

namespace Nemerle.Compiler.Backend
{
  public module Extentions
  {
    public GetSystemType(this type : TypeVar) : System.Type
    {
      SystemHelpers.GetSystemType(type)
    }
    
    public GetSystemType(this type : FixedType) : System.Type
    {
      SystemHelpers.GetSystemType(type)
    }
  }
}