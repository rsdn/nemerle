class M {
  fld : int;
  meth () : void {}
  static f () : void { def x = 1; x <- M }	// E: `M' does not refer to value
  static g () : void { def x = 1; x <- M.fld }	// E: `M.fld' does not refer to value
  static h () : void { def x = 1; x <- M.meth }	// E: `M.meth' does not refer to value
}
