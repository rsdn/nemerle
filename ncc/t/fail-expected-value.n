class M {
  field fld : int;
  method meth () : void = ();
  fun f () : void = let x = 1 in x <- M;	// E: `M' does not refer to value
  fun g () : void = let x = 1 in x <- M.fld;	// E: `M.fld' does not refer to value
  fun h () : void = let x = 1 in x <- M.meth;	// E: `M.meth' does not refer to value
}
