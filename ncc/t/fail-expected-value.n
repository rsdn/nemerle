class M {
  fld : int;
  meth () : void = ();
  static f () : void = let x = 1 in x <- M;	// E: `M' does not refer to value
  static g () : void = let x = 1 in x <- M.fld;	// E: `M.fld' does not refer to value
  static h () : void = let x = 1 in x <- M.meth;	// E: `M.meth' does not refer to value
}
