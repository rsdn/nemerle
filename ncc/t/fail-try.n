class M {
  fun f () : int =
    try
      1
    with ?x : System.Exception =>
      "foo";	// E: try...with handler was expected to have type.*
      
  fun g () : int = try 1 finally "foo"; // E: try...finally handler was expected to have type void
}
