class M {
  static f () : int =
    try
      1
    with x : System.Exception =>
      "foo";	// E: try...with handler was expected to have type.*
      
  static g () : int = try 1 finally "foo"; // E: try...finally handler was expected to have type void
}
