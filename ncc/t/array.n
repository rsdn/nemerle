
public class ArrayTest
{
	public static Main () : void 
	{
		print_endline ("Array test.");

		mutable a <- array [1; 2; 3];
		mutable b <- array ["ala"; "ma"; "kota"];

		mutable x <- 2;
		mutable y <- 3;

		mutable c <- array [x; y; x + y; x - y; if (x < y) x else y];

		()
	}
}

(*
BEGIN-OUTPUT
Array test.
END-OUTPUT
*)
