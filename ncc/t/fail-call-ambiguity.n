open M1;
open M2;

interface I1 {}
interface I2 {}
interface I implements I1, I2 {}

class M {
  fun f (a : I1) : void = ();
  fun f (a : I2) : void = ();
  fun m (a : I) : void  = M.f (a);	// E: ambiguity in call
}

class M1 {
  fun g (a : I) : void = ();
}

class M2 {
  fun g (a : I) : void = ();
}

class M3 {
  fun h (a : I) : void = g(a);	// E: ambiguity in call
}
