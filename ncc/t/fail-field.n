class X ('a) {
  ref x : 'a;
  this () {}
}

class M {
  static Main () : void {
    let x = X () in
    {
      x # x <- "foo";   // OK
      x # x <- 3;       // E: assigned value was expected to have type 
    }
  }
}
