class X ('a) {
  field ref x : 'a;
  method this () {}
}

class M {
  fun Main () : void {
    let x = X () in
    {
      x # x <- "foo";   // OK
      x # x <- 3;       // E: assigned value was expected to have type 
    }
  }
}
