class X ('a) {
  field ref x : 'a;
  method this () {}
}

class M {
  fun Main () : void {
    let x = X () in
    {
      x # x <- "foo";
      x # x <- 3;       // E: type clash in assignment string <- int
    }
  }
}
