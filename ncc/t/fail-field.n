class X ('a) {
  mutable x : 'a;
  this () {}
}

class M {
  static Main () : void {
    def x = X ();
    x . x <- "foo";   // OK
    x . x <- 3;       // E: assigned value was expected to have type 
  }
}
