module Foo {

  'a foo (a : 'a) : 'a
    {
      def 'b bar (a : 'a, b : 'b) : 'a * 'b = 
        ((a : 'a), (b : 'b));
      def (x, y) = bar (a, 1);
      def (x, y) = bar (a, "qux");
      x
    }
  
}
