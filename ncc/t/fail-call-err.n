class M {
  fun f () : void = print_string (1);	// E: in arg #1 needed.*

  fun g () : void = 1 ();	// E: called value has non-functional type .*

  fun h () : void = M.g(2);	// E: .*needed 0 parameters, got 1
}
