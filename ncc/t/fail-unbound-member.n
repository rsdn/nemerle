class C {
  mutable x : int;
  m () : void {}
}

class M {
  static main () : void {
    def c = (null : C);
    c.x <- 3; // OK
    3;	// W: ignored .*value
    c.y <- 3	// E: unbound member `y' in type `C'
  }

  static f (c : C) : void { c.m <- 3 }	// E: member `m' was expected to be a field, not method
}
