
module M {
  public Main () : void {
    pretty_print_expr (printf ("ka%d%smil", 5, "ss"));
    pretty_print_expr ( 
      def loop (acc : int, x : list ('a)) : int {
        match (x) {
          | _ :: xs => loop (acc + 1, xs)
          | _ => acc
        }
      }
    );
    pretty_print_expr (  
      if ( n == 0 )
        h
      else
        nth(t, n-1)
    );
    pretty_print_expr ( fold_left(append, [], rev(l)) );
  }
}
