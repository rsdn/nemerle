class M {
  field fld : int;
  method meth () : void = ();
  fun f () : void = let x = 1 in x <- M;	// E: expected value reference, but `M' is type
  fun g () : void = let x = 1 in x <- M.fld;	// E: expected value, got field `fld'
  fun h () : void = let x = 1 in x <- M.meth;	// E: expected value, got method `meth'
}
