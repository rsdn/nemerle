* grep FIXME *

* make if use match not tymatch
* check exhastuveness of match

Known issues, not likely to be fixed in this compiler:
* check exhaustiveness of tymatch
* attributes are not handled at all
* arrays are not handled at all
