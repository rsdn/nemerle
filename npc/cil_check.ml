let validate _decls = ()

(* FIXME: check lists of same-named functions/methods if they
   have different types. *)
   
(* FIXME: check if structs only contains what they can. *)

(* FIXME: extends, implements: check if what we implement are interfaces, 
   we are class, and what we extend is a class. *)

