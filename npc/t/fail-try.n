class M {
  fun f () : int =
    try
      1
    with (?x : System.Exception)
      "foo";	// E: types in `try' and `with' sections are not compatible

  fun g () : int = try 1 finally "foo"; // E: `finally' expression should have void type, not string
}
