class M {
  fld : int;
  meth () : void = ();
  static f () : void = let x = 1 in x <- M;	// E: expected value reference, but `M' is type
  static g () : void = let x = 1 in x <- M.fld;	// E: expected value, got field `fld'
  static h () : void = let x = 1 in x <- M.meth;	// E: expected value, got method `meth'
}
