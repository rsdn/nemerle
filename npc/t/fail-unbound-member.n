class C {
  field x : int;
  method m () : void = ();
}

class M {
  fun main () : void =
    let c = (null : C) in
    {
      c#x <- 3;
      3;	// E: expressions in middle of sequence need to have void type, not int
      c#y <- 3	// E: type `C' has no member named `y'
    };

  fun f (c : C) : void = c#m <- 3;	// E: m was expected to be field, not method
}
