<config>
  <server>

    <root>.</root>

    <port>8000</port>

    <webapps_dir>webapps</webapps_dir>

    <url_rewriting>false</url_rewriting>

    <max_connections>100</max_connections>

    <uploads_dir>uploads</uploads_dir>

  </server>
</config>
