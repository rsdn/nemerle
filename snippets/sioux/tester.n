using Nemerle.Xml;
using System.Xml;

module M {

  Main () : void
  {
    def tmpl = XmlTemplate ("test.xml");
    tmpl.SetText ("foo", "blah, bar, baz");
    tmpl.SetText ("foo/bar", "baz");
    tmpl.GetDocument ().Save (System.Console.Out);
    
  }
}
